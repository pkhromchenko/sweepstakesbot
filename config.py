# Dict to store access tokens
twitterAPI_config = {
  "consumer_key":
  
}

# Keywords to define as a giveaway
search_values = ["giveaway", "nftgiveaway", "to win", "to enter", "sweepstakes", "giveaways"]

toggle_msgs = False

# Triggers for bot actions
like_triggers = ["like", "favorite"]

dm_triggers = ["dm", "message"]

reply_triggers = ["comment", ]

rt_triggers = ["retweet", "rt", "share"]

follow_triggers = ["follow"]

tag_triggers = ["tag"]
